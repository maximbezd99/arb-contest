# Bridge-network overlay: containers join the user-defined `arena` network and
# resolve each other by service name (e.g. `simulation:9003`). Works on macOS
# Docker Desktop and on Linux.
services:
  simulation:
    networks:
      - arena
  example-bot:
    networks:
      - arena

networks:
  arena:
    driver: bridge
