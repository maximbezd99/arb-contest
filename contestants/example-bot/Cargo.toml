[package]
name = "example-bot"
version = "0.1.0"
edition = "2021"
rust-version = "1.80"

[dependencies]
nix = { version = "0.29", features = ["socket", "net"] }

[profile.release]
lto = "thin"
codegen-units = 1
